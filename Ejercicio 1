#include <stdio.h>

//Lista de numeros negativos con positivos 

int main (){

	int i, num;
	
	printf("De cuantos numeros quieres tu serie:");
	scanf("%d", &num);
	
	for (i=1; i<=num; i++){
		printf("%d\n",i);
		printf("%d\n",-i);
		
	}
	
	return 0;
}
